# WalkCroach fork-only paths (NFR-F13)
#
# DENY BY DEFAULT. Any file under vscode/ that differs from the upstream pin and
# is not matched below fails `npm run audit:surface-area`.
#
# Do not add a "forbidden" section. This file previously ended with:
#
#     ## Explicitly forbidden to diverge without decision-log entry
#     src/vs/code/**
#     src/vs/platform/**
#     src/vs/base/**
#     src/vs/editor/**
#
# `loadAllowlist()` skips only lines beginning with `#`, so the heading was a
# comment but the four paths under it were parsed as ALLOW globs — inverting
# their meaning and silently permitting edits anywhere in four of the largest
# upstream trees. Removed 2026-08-05. Anything absent from this file is already
# forbidden; saying so in the file is what broke it.

## Product code — additive, never conflicts on an upstream carry

# Workbench UI: views, panes, commands, settings.
src/vs/workbench/contrib/walkcroach/**

# Agent Host provider (D2). Cannot live in contrib/: `IAgent` is a
# platform-layer contract and VS Code layering forbids platform/ importing
# workbench/, so it sits beside upstream's own claude/, codex/ and copilot/.
src/vs/platform/agentHost/node/walkcroach/**

## Minimal hooks — must stay tiny, review on every upstream sync

# Generated by scripts/apply-product.mjs from product/product.walkcroach.json.
product.json
product.json.oss.bak

# Single import that loads the workbench contribution.
src/vs/workbench/workbench.common.main.ts

# Registers WalkCroachAgent with the Agent Host — one import, one call.
# Added 2026-08-05 (D2): a provider cannot be contributed any other way short of
# implementing an AHP server.
src/vs/platform/agentHost/node/agentHostMain.ts

.gitignore
