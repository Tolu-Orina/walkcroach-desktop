# Upstream conflict logs

Sync (`scripts/sync-upstream.sh`) writes dated `*-conflicts.md` here when a merge conflicts.

Cadence records stay in `cadence/records/`. Product docs live only in `docs/{ARCHITECTURE,STATUS,SHIPPING}.md`.
